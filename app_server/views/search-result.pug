extends layout

block content
  .row.banner
    .col-12
      h1= title
  .row
    .col-12.col-md-8
      each food in foods
        .card
          .card-block
            a(href="/food")=food.name
            p=food.id
            p=food.url
